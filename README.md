By# git_commit_money
git remote add origin https://github.com/477447/git_commit_money.git

git push -u origin master
commit money
commit_amesropyan61@yandex.ru


README.md

README.md

.github/workflows/blank.yml
strategy:
  fail-fast: false
  matrix:
    language: [  ]
    # CodeQL supports [ 'cpp', 'csharp', 'go', 'java', 'javascript', 'python', 'ruby' ]
    # Use only 'java' to analyze code written in Java, Kotlin or both
    # Use only 'javascript' to analyze code written in JavaScript, TypeScript or both
    # Learn more about CodeQL language support at https://aka.ms/codeql-docs/language-support

steps:
- name: Checkout repository
  uses: actions/checkout@v3

# Initializes the CodeQL tools for scanning.
- name: Initialize CodeQL
  uses: github/codeql-action/init@v2
  with:
    languages: ${{ matrix.language }}
    # If you wish to specify custom queries, you can do so here or in a config file.
    # By default, queries listed here will override any specified in a config file.
    # Prefix the list here with "+" to use these queries and those in the config file.

    # Details on CodeQL's query packs refer to : https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
    # queries: security-extended,security-and-quality


# Autobuild attempts to build any compiled languages  (C/C++, C#, Go, or Java).
# If this step fails, then you should remove it and run the build manually (see below)
- name: Autobuild
  uses: github/codeql-action/autobuild@v2

# ℹ️ Command-line programs to run using the OS shell.
# 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun

#   If the Autobuild fails above, remove it and uncomment the following three lines.
#   modify them (or add more) to build your code if your project, please refer to the EXAMPLE below for guidance.

# - run: |
#   echo "Run, Build Application using script"
#   ./location_of_script_within_repo/buildscript.sh

- name: Perform CodeQL Analysis
  uses: github/codeql-action/analyze@v2
  with:
    category: "/language:${{matrix.language}}"
[ubuntu-latest, windows-latest]

README.md

porcupineyhairs:TemplateInjection

README.md



print(os.environ['PATH'])

jobs.<job_id>.steps[*].run
# меня зовут ANUSHAVAN MESROBYAN

# Всё банки Армении блокируют мои счёты
# и даже центральный Банк Армении

# очень важно чтобы на моей странице
# появилась специальное приложение

# чтобы при помощи этого приложения
# я смог заполнить данные моей банковской

# карты и получать крупные суммы
# должен отметить что все эти банки

# выводили крупные суммы из моих счетов
# например когда я требовал чтобы

# они мне выдали платную выписку из моих
# счётов они отказывают мне что ничего

# подобного не существует затем после
# того как только я выхожу из банка

#они выводили эти суммы в ихние счёты
# в виде ценных бумаг и Акций

README.md

.github/workflows/blank.yml
.github/workflows/blank.yml

Перейти к основному контенту
Эта статья также доступна на упрощенном китайском языке.
Документы GitHub
Настройка сканирования кода
В этой статье
О настройке сканирования кода
Редактирование рабочего процесса сканирования кода
Настройка частоты
Сканирование при нажатии
Сканирование запросов на извлечение
Определение серьезности, вызывающей сбой проверки запроса на извлечение
Избегание ненужных проверок запросов на извлечение
Сканирование по расписанию
Пример
Указание операционной системы
Указание местоположения для баз данных CodeQL
Изменение анализируемых языков
Анализ зависимостей Python
Настройка категории для анализа
Выполнение дополнительных запросов
Использование пакетов запросов CodeQL
Загрузка пакетов CodeQL с корпоративного сервера GitHub
Использование запросов в пакетах QL
Работа с пользовательскими файлами конфигурации
Использование пользовательского файла конфигурации
Задание пакетов запросов CodeQL
Указание дополнительных запросов
Отключение запросов по умолчанию
Исключение определенных запросов из анализа
Указание каталогов для сканирования
Примеры файлов конфигурации
Настройка сканирования кода для скомпилированных языков
Загрузка данных сканирования кода в GitHub
Вы можете настроить, как GitHub сканирует код в вашем проекте на наличие уязвимостей и ошибок.

Кто может использовать эту функцию
Пользователи с правами на запись в репозиторий могут настроить сканирование кода для репозитория.

Сканирование кода доступно для всех общедоступных репозиториев на GitHub.com . Сканирование кода также доступно для частных репозиториев, принадлежащих организациям, использующим GitHub Enterprise Cloud и имеющим лицензию на GitHub Advanced Security. Дополнительные сведения см. в разделе "О расширенной безопасности GitHub".

О настройке сканирования кода
Вы можете запустить сканирование кода на GitHub, используя GitHub Actions или из вашей системы непрерывной интеграции (CI). Дополнительные сведения см. в разделах "О действиях GitHub" или "О сканировании кода CodeQL в вашей системе CI".

В этой статье рассказывается о запуске сканирования кода на GitHub с помощью действий.

Прежде чем вы сможете настроить сканирование кода для репозитория, необходимо настроить сканирование кода, добавив в репозиторий рабочий процесс GitHub Actions. Дополнительные сведения см. в разделе "Настройка сканирования кода для репозитория".

Как правило, вам не нужно редактировать рабочий процесс по умолчанию для сканирования кода. Однако при необходимости вы можете отредактировать рабочий процесс, чтобы настроить некоторые параметры. Например, вы можете отредактировать рабочий процесс анализа CodeQL в GitHub, чтобы указать частоту проверок, языки или каталоги для сканирования, а также то, что проверка кода CodeQL ищет в вашем коде. Вам также может потребоваться отредактировать рабочий процесс анализа CodeQL, если вы используете определенный набор команд для компиляции своего кода.

Анализ CodeQL - это всего лишь один из видов сканирования кода, который вы можете выполнить в GitHub. GitHub Marketplace содержит другие рабочие процессы сканирования кода, которые вы можете использовать. Вы можете найти некоторые из них на странице "Начало работы со сканированием кода", доступ к которой можно получить на вкладке Безопасность. Конкретные примеры, приведенные в этой статье, относятся к файлу рабочего процесса анализа CodeQL.

Редактирование рабочего процесса сканирования кода
GitHub сохраняет файлы рабочего процесса в каталоге .github/workflows вашего репозитория. Вы можете найти добавленный вами рабочий процесс, выполнив поиск по имени его файла. Например, по умолчанию файл рабочего процесса для сканирования кода CodeQL называется codeql-analysis.yml.

В вашем репозитории перейдите к файлу рабочего процесса, который вы хотите отредактировать.
В правом верхнем углу представления файлов, чтобы открыть редактор рабочего процесса, щелкните .
Кнопка Изменить файл рабочего процесса
После того как вы отредактировали файл, нажмите кнопку Начать фиксацию и заполните форму "Зафиксировать изменения". Вы можете выбрать фиксацию непосредственно в текущей ветке или создать новую ветку и запустить запрос на извлечение.
Зафиксируйте обновление в рабочем процессе codeql.yml
Дополнительные сведения о редактировании файлов рабочего процесса см. в разделе "Изучение действий GitHub".

Настройка частоты
Вы можете настроить рабочий процесс анализа CodeQL для сканирования кода по расписанию или при возникновении определенных событий в репозитории.

Сканирование кода, когда кто-то вносит изменения, и всякий раз, когда создается запрос на извлечение, предотвращает внесение разработчиками новых уязвимостей и ошибок в код. Сканирование кода по расписанию информирует вас о последних уязвимостях и ошибках, обнаруженных GitHub, исследователями безопасности и сообществом, даже когда разработчики не ведут активную поддержку репозитория.

Сканирование при нажатии
По умолчанию рабочий процесс анализа CodeQL использует on.pushсобытие для запуска сканирования кода при каждом отправлении в ветвь репозитория по умолчанию и любые защищенные ветви. Для запуска сканирования кода в указанной ветке рабочий процесс должен существовать в этой ветке. Дополнительные сведения см. в разделе "Синтаксис рабочего процесса для действий GitHub".

Если вы выполняете сканирование с помощью push, результаты отображаются на вкладке Безопасность вашего репозитория. Дополнительные сведения см. в разделе "Управление оповещениями о сканировании кода для вашего репозитория".

Кроме того, когда on:pushсканирование возвращает результаты, которые могут быть сопоставлены с открытым запросом на извлечение, эти предупреждения будут автоматически отображаться в запросе на извлечение в тех же местах, что и другие предупреждения о запросах на извлечение. Предупреждения идентифицируются путем сравнения существующего анализа главы филиала с анализом для целевого филиала. Дополнительные сведения об оповещениях о проверке кода в запросах на извлечение см. в разделе "Сортировка предупреждений о проверке кода в запросах на извлечение".

Сканирование запросов на извлечение
Рабочий процесс анализа CodeQL по умолчанию использует pull_requestсобытие для запуска сканирования кода при извлечении запросов, направленных против ветви по умолчанию. Если запрос на извлечение поступает из частного разветвления, pull_requestсобытие будет запущено, только если вы выбрали опцию "Запускать рабочие процессы из запросов на извлечение разветвления" в настройках репозитория. Дополнительные сведения см. в разделе "Управление настройками действий GitHub для репозитория".

Дополнительные сведения о pull_requestсобытии см. в разделе "События, запускающие рабочие процессы".

Если вы сканируете запросы на извлечение, результаты отображаются в виде предупреждений при проверке запроса на извлечение. Дополнительные сведения см. в разделе "Сортировка предупреждений о сканировании кода в запросах на извлечение".

Использование pull_requestтриггера, настроенного на сканирование фиксации слияния запроса на извлечение, а не фиксации заголовка, даст более эффективные и точные результаты, чем сканирование заголовка ветки при каждом нажатии. Однако, если вы используете систему CI / CD, которая не может быть настроена для запуска запросов на извлечение, вы все равно можете использовать on:pushтриггер, и сканирование кода сопоставит результаты с открытыми запросами на извлечение в ветке и добавит предупреждения в качестве аннотаций к запросу на извлечение. Для получения дополнительной информации см. раздел "Сканирование при нажатии".

Определение серьезности, вызывающей сбой проверки запроса на извлечение
По умолчанию только предупреждения с уровнем серьезности Errorили уровнем серьезности безопасности Criticalили Highвызовут сбой проверки запроса на извлечение, и проверка все равно будет успешной с предупреждениями меньшей степени серьезности. Вы можете изменить уровни серьезности предупреждений и строгости безопасности, которые приведут к сбою проверки запроса на извлечение, в настройках вашего репозитория. Дополнительные сведения об уровнях серьезности см. в разделе "Об оповещениях при сканировании кода".

Вкл. GitHub.com , перейдите на главную страницу репозитория.

Под именем вашего репозитория нажмите  Настройки.
Кнопка Настройки репозитория

В разделе "Безопасность" боковой панели щелкните Безопасность и анализ кода.

В разделе "Сканирование кода", справа от "Сбой проверки", используйте выпадающее меню, чтобы выбрать уровень серьезности, который вы хотели бы вызвать сбой проверки запроса на извлечение.
Проверьте настройку сбоя

Избегание ненужных проверок запросов на извлечение
Возможно, вы захотите избежать запуска сканирования кода при определенных запросах на извлечение, направленных на ветку по умолчанию, независимо от того, какие файлы были изменены. Вы можете настроить это, указав on:pull_request:paths-ignoreили on:pull_request:pathsв рабочем процессе сканирования кода. Например, если единственные изменения в запросе на извлечение относятся к файлам с расширениями файлов.md, или .txtвы можете использовать следующий paths-ignoreмассив.

on:
  push:
    branches: [main, protected]
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
Примечания

on:pull_request:paths-ignore и on:pull_request:pathsзадайте условия, которые определяют, будут ли действия в рабочем процессе выполняться при запросе на извлечение. Они не определяют, какие файлы будут проанализированы при выполнении действий. Если запрос на извлечение содержит какие-либо файлы, которые не совпадают с on:pull_request:paths-ignoreor on:pull_request:paths, рабочий процесс выполняет действия и сканирует все файлы, измененные в запросе на извлечение, включая файлы, совпадающие с on:pull_request:paths-ignoreor on:pull_request:paths, если только файлы не были исключены. Для получения информации о том, как исключить файлы из анализа, см. раздел "Указание каталогов для сканирования".
Для файлов рабочего процесса сканирования кода CodeQL не используйте paths-ignorepathsключевые слова или с on:pushсобытием, поскольку это может привести к отсутствию анализа. Для получения точных результатов сканирование кода CodeQL должно иметь возможность сравнивать новые изменения с анализом предыдущего коммита.
Дополнительные сведения об использовании on:pull_request:paths-ignoreи on:pull_request:pathsопределении того, когда рабочий процесс будет выполняться для запроса на извлечение, см. в разделе "Синтаксис рабочего процесса для действий GitHub".

Сканирование по расписанию
Если вы используете рабочий процесс анализа CodeQL по умолчанию, рабочий процесс будет сканировать код в вашем репозитории раз в неделю в дополнение к проверкам, инициируемым событиями. Чтобы настроить это расписание, отредактируйте cronзначение в рабочем процессе. Дополнительные сведения см. в разделе "Синтаксис рабочего процесса для действий GitHub".

Примечание: GitHub запускает только запланированные задания, которые находятся в рабочих процессах в ветке по умолчанию. Изменение расписания в рабочем процессе в любой другой ветке не имеет никакого эффекта, пока вы не объедините ветку с веткой по умолчанию.

Пример
В следующем примере показан рабочий процесс анализа CodeQL для конкретного репозитория, в котором вызывается ветвь по умолчанию mainи вызывается одна защищенная ветвь protected.

on:
  push:
    branches: [main, protected]
  pull_request:
    branches: [main]
  schedule:
    - cron: '20 14 * * 1'
Этот рабочий процесс сканирует:

Каждое нажатие на ветку по умолчанию и защищенную ветку
Каждый запрос на извлечение в ветку по умолчанию
Ветка по умолчанию каждый понедельник в 14:20 UTC
Указание операционной системы
Если для компиляции вашего кода требуется определенная операционная система, вы можете настроить операционную систему в своем рабочем процессе анализа CodeQL. Измените значениеjobs.analyze.runs-on, чтобы указать операционную систему для компьютера, на котором выполняются действия сканирования кода.

jobs:
  analyze:
    name: Analyze
    runs-on: [ubuntu-latest]
Если вы решите использовать автономный бегун для сканирования кода, вы можете указать операционную систему, используя соответствующую метку в качестве второго элемента в массиве из двух элементов, после self-hosted.

jobs:
  analyze:
    name: Analyze
    runs-on: [self-hosted, ubuntu-latest]
Сканирование кода CodeQL поддерживает последние версии Ubuntu, Windows и macOS. Поэтому типичными значениями для этого параметра являются: ubuntu-latest, windows-latest, и macos-latest. Дополнительные сведения см. в разделах "Выбор бегуна для задания" и "Использование меток с автономными бегунами".

Если вы используете автономный бегун, вы должны убедиться, что Git находится в переменной PATH . Дополнительные сведения см. в разделах "О самостоятельных бегунах" и "Добавление самостоятельных бегунов".

Рекомендуемые спецификации (ОЗУ, ядра процессора и диск) для выполнения анализа CodeQL на автономных компьютерах см. в разделе "Рекомендуемые аппаратные ресурсы для запуска CodeQL".

Указание местоположения для баз данных CodeQL
В общем, вам не нужно беспокоиться о том, где рабочий процесс анализа CodeQL размещает базы данных CodeQL, поскольку последующие шаги автоматически найдут базы данных, созданные предыдущими шагами. Однако, если вы пишете пользовательский этап рабочего процесса, который требует, чтобы база данных CodeQL находилась в определенном расположении на диске, например, для загрузки базы данных в качестве артефакта рабочего процесса, вы можете указать это местоположение, используя db-locationпараметр в initдействии.

- uses: github/codeql-action/init@v2
  with:
    db-location: '${{ github.workspace }}/codeql_dbs'
Рабочий процесс анализа CodeQL ожидает, что путь, указанный вdb-location, будет доступен для записи и либо не существует, либо является пустым каталогом. При использовании этого параметра в задании, выполняемом на самостоятельном сервере runner или использующем контейнер Docker, пользователь несет ответственность за то, чтобы выбранный каталог очищался между запусками или чтобы базы данных удалялись, когда они больше не нужны. Это не обязательно для заданий, выполняемых на бегунах, размещенных на GitHub, которые при каждом запуске получают новый экземпляр и чистую файловую систему. Для получения дополнительной информации см. раздел "О бегунах, размещенных на GitHub".

Если этот параметр не используется, рабочий процесс анализа CodeQL создаст базы данных во временном расположении по своему выбору.

Изменение анализируемых языков
Сканирование кода CodeQL автоматически обнаруживает код, написанный на поддерживаемых языках.

C / C++
C#
Вперед
Java/Kotlin
JavaScript /TypeScript
Python
Ruby
Примечания:

Анализ CodeQL для Kotlin в настоящее время находится в стадии бета-тестирования. Во время бета-тестирования анализ Kotlin будет менее полным, чем анализ CodeQL на других языках.
Используется javaдля анализа кода, написанного на Java, Kotlin или на обоих языках.
Используется javascriptдля анализа кода, написанного на JavaScript, TypeScript или обоих.
Для получения дополнительной информации см. Документацию на веб-сайте CodeQL: "Поддерживаемые языки и фреймворки".

Файл рабочего процесса анализа CodeQL по умолчанию содержит вызываемую матрицуlanguage, в которой перечислены языки в вашем репозитории, которые анализируются. CodeQL автоматически заполняет эту матрицу при добавлении сканирования кода в репозиторий. Использование languageматрицы оптимизирует CodeQL для параллельного выполнения каждого анализа. Мы рекомендуем, чтобы все рабочие процессы приняли эту конфигурацию из-за преимуществ производительности распараллеливания сборок. Дополнительные сведения о матрицах см. в разделе "Использование матрицы для ваших заданий".

Если ваш репозиторий содержит код более чем на одном из поддерживаемых языков, вы можете выбрать, какие языки вы хотите проанализировать. Существует несколько причин, по которым вы можете запретить анализ языка. Например, проект может иметь зависимости на языке, отличном от основного текста вашего кода, и вы можете предпочесть не видеть оповещения для этих зависимостей.

Если ваш рабочий процесс использует languageматрицу, то CodeQL жестко запрограммирован для анализа только языков в матрице. Чтобы изменить языки, которые вы хотите проанализировать, отредактируйте значение переменной matrix . Вы можете удалить язык, чтобы предотвратить его анализ, или добавить язык, которого не было в репозитории при настройке сканирования кода. Например, если при настройке сканирования кода репозиторий изначально содержал только JavaScript, а позже вы добавили код на Python, вам нужно будет добавить pythonв матрицу.

jobs:
  analyze:
    name: Analyze
    ...
    strategy:
      fail-fast: false
      matrix:
        language: ['javascript', 'python']
Если ваш рабочий процесс не содержит вызываемой матрицыlanguage, то CodeQL настроен на последовательный запуск анализа. Если вы не укажете языки в рабочем процессе, CodeQL автоматически обнаружит и попытается проанализировать все поддерживаемые языки в репозитории. Если вы хотите выбрать, какие языки анализировать, без использования матрицы, вы можете использовать languagesпараметр в initдействии.

- uses: github/codeql-action/init@v2
  with:
    languages: cpp, csharp, python
Анализ зависимостей Python
Для размещенных на GitHub бегунов, которые используют только Linux, рабочий процесс анализа CodeQL попытается автоматически установить зависимости Python, чтобы получить больше результатов для анализа CodeQL. Вы можете управлять этим поведением, указав setup-python-dependenciesпараметр для действия, вызываемого шагом "Инициализировать CodeQL". По умолчанию для этого параметра установлено значениеtrue:

Если репозиторий содержит код, написанный на Python, на шаге "Инициализировать CodeQL" устанавливаются необходимые зависимости для бегуна, размещенного на GitHub. Если автоматическая установка прошла успешно, действие также устанавливает переменную среды CODEQL_PYTHONв исполняемый файл Python, который включает зависимости.

Если в репозитории нет зависимостей от Python или зависимости указаны неожиданным образом, вы получите предупреждение, и действие будет продолжено с оставшимися заданиями. Действие может выполняться успешно, даже если возникают проблемы с интерпретацией зависимостей, но результаты могут быть неполными.

Кроме того, вы можете установить зависимости Python вручную в любой операционной системе. Вам нужно будет добавить setup-python-dependenciesи установить для него значение false, а также установить CODEQL_PYTHONдля исполняемого файла Python, который включает зависимости, как показано в этом извлечении рабочего процесса:

jobs:
  CodeQL-Build:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ];
          then pip install -r requirements.txt;
          fi
          # Set the `CODEQL-PYTHON` environment variable to the Python executable
          # that includes the dependencies
          echo "CODEQL_PYTHON=$(which python)" >> $GITHUB_ENV
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: python
          # Override the default behavior so that the action doesn't attempt
          # to auto-install Python dependencies
          setup-python-dependencies: false
Настройка категории для анализа
Используется categoryдля различения нескольких анализов для одного и того же инструмента и фиксации, но выполняемых на разных языках или в разных частях кода. Категория, которую вы укажете в своем рабочем процессе, будет включена в файл результатов SARIF.

Этот параметр особенно полезен, если вы работаете с monorepos и имеете несколько файлов SARIF для разных компонентов monorepo.

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
      with:
        # Optional. Specify a category to distinguish between multiple analyses
        # for the same tool and ref. If you don't use `category` in your workflow,
        # GitHub will generate a default category name for you
        category: "my_category"
Если вы не укажете categoryпараметр в своем рабочем процессе, GitHub сгенерирует для вас имя категории на основе имени файла рабочего процесса, запускающего действие, имени действия и любых переменных матрицы. Например:

Рабочий .github/workflows/codeql-analysis.ymlпроцесс и analyzeдействие создадут категорию .github/workflows/codeql.yml:analyze.
Рабочий .github/workflows/codeql-analysis.ymlпроцесс, analyzeдействие и {language: javascript, os: linux}переменные матрицы создадут категорию .github/workflows/codeql-analysis.yml:analyze/language:javascript/os:linux.
categoryЗначение будет отображаться как <run>.automationDetails.idсвойство в SARIF версии v2.1.0. Дополнительные сведения см. в разделе "Поддержка SARIF для сканирования кода".

Указанная вами категория не будет перезаписывать сведения об runAutomationDetailsобъекте в файле SARIF, если он включен.

Выполнение дополнительных запросов
Когда вы используете CodeQL для сканирования кода, механизм анализа CodeQL генерирует базу данных из кода и выполняет к ней запросы. Анализ CodeQL использует набор запросов по умолчанию, но вы можете указать дополнительные запросы для выполнения в дополнение к запросам по умолчанию.

Вы также можете указать запросы, которые хотите исключить из анализа или включить в анализ. Для этого требуется использование пользовательского файла конфигурации. Дополнительные сведения см. в разделах "Использование пользовательского файла конфигурации" и "Исключение определенных запросов из анализа" ниже.

Вы можете запускать дополнительные запросы, если они являются частью пакета CodeQL (бета-версия), опубликованного в реестре контейнеров GitHub, или пакета QL, хранящегося в репозитории. Для получения дополнительной информации см. раздел "О сканировании кода с помощью CodeQL".

Для указания дополнительных запросов, которые вы хотите выполнить, доступны следующие параметры:

packs чтобы установить один или несколько пакетов запросов CodeQL (бета-версия) и запустить набор запросов по умолчанию или запросы для этих пакетов.
queries чтобы указать один .ql-файл, каталог, содержащий несколько .файлы ql, файл определения набора запросов .qls или любая комбинация. Дополнительные сведения об определениях наборов запросов см. в разделе "Создание наборов запросов CodeQL".
Вы можете использовать оба packsи queriesв одном рабочем процессе.

Мы не рекомендуем ссылаться на наборы запросов непосредственно из github/codeqlрепозитория, например, github/codeql/cpp/ql/src@main. Такие запросы должны быть перекомпилированы и могут быть несовместимы с версией CodeQL, которая в настоящее время активна в GitHub Actions, что может привести к ошибкам во время анализа.

Использование пакетов запросов CodeQL
Примечание: Функциональность управления пакетами CodeQL, включая пакеты CodeQL, в настоящее время находится в стадии бета-тестирования и может быть изменена.

Чтобы добавить один или несколько пакетов запросов CodeQL (бета-версия), добавьте with: packs:запись в uses: github/codeql-action/init@v2раздел рабочего процесса. Внутри packsвы указываете один или несколько пакетов для использования и, при необходимости, версию для загрузки. Если вы не указываете версию, загружается последняя версия. Если вы хотите использовать пакеты, которые не являются общедоступными, вам необходимо установить GITHUB_TOKENдля переменной среды значение secret, которое имеет доступ к пакетам. Дополнительные сведения см. в разделах "Проверка подлинности в рабочем процессе" и "Зашифрованные секреты".

Примечание: Для рабочих процессов, которые генерируют базы данных CodeQL для нескольких языков, вы должны вместо этого указать пакеты запросов CodeQL в файле конфигурации. Для получения дополнительной информации см. раздел "Указание пакетов запросов CodeQL" ниже.

В приведенном ниже примере scopeуказана организация или личная учетная запись, опубликовавшая пакет. При запуске рабочего процесса с GitHub загружаются четыре пакета запросов CodeQL и выполняются запросы по умолчанию или набор запросов для каждого пакета:

Загружается последняя версия pack1и выполняются все запросы по умолчанию.
Загружается версия 1.2.3 pack2и выполняются все запросы по умолчанию.
Загружается последняя версияpack3, совместимая с версией 3.2.1, и выполняются все запросы.
Загружается версия 4.5.6pack4, и выполняются только запросы, найденные в.path/to/queries
- uses: github/codeql-action/init@v2
  with:
    # Comma-separated list of packs to download
    packs: scope/pack1,scope/pack2@1.2.3,scope/pack3@~3.2.1,scope/pack4@4.5.6:path/to/queries
Загрузка пакетов CodeQL с корпоративного сервера GitHub
Если в вашем рабочем процессе используются пакеты, опубликованные при установке GitHub Enterprise Server, вам необходимо указать своему рабочему процессу, где их найти. Вы можете сделать это, используя registriesввод действия github / codeql-action/init@v2 . Этот ввод принимает список url, packages, и tokenсвойств, как показано ниже.

- uses: github/codeql-action/init@v2
  with:
    registries: |
      # URL to the container registry, usually in this format
      - url: https://containers.GHEHOSTNAME1/v2/

        # List of package glob patterns to be found at this registry
        packages:
          - my-company/*
          - my-company2/*

        # Token, which should be stored as a secret
        token: ${{ secrets.GHEHOSTNAME1_TOKEN }}

      # URL to the default container registry
      - url: https://ghcr.io/v2/
        # Packages can also be a string
        packages: "*/*"
        token: ${{ secrets.GHCR_TOKEN }}

    
Шаблоны пакетов в списке реестров рассматриваются по порядку, поэтому, как правило, сначала следует размещать наиболее конкретные шаблоны пакетов. Значения для tokenдолжны быть личным токеном доступа (classic), сгенерированным экземпляром GitHub, с которого вы загружаете с read:packagesразрешения.

Обратите внимание на |после имени registriesсвойства. Это важно, поскольку входные данные GitHub Actions могут принимать только строки. Использование |преобразует последующий текст в строку, которая позже анализируется действием github / codeql-action / init @v2.

Использование запросов в пакетах QL
Чтобы добавить один или несколько запросов, добавьте with: queries:запись в uses: github/codeql-action/init@v2раздел рабочего процесса. Если запросы находятся в частном репозитории, используйте external-repository-tokenпараметр, чтобы указать токен, который имеет доступ к проверке частного репозитория.

- uses: github/codeql-action/init@v2
  with:
    queries: COMMA-SEPARATED LIST OF PATHS
    # Optional. Provide a token to access queries stored in private repositories.
    external-repository-token: ${{ secrets.ACCESS_TOKEN }}
Вы также можете указать наборы запросов в значении queries. Наборы запросов представляют собой наборы запросов, обычно сгруппированных по назначению или языку.

Следующие наборы запросов встроены в CodeQL code scanning и доступны для использования.

Набор запросов	Описание
security-extended	Запросы из набора по умолчанию, а также запросы с меньшей серьезностью и точностью
security-and-quality	Запросы отsecurity-extended, а также запросы на удобство обслуживания и надежность
Когда вы указываете набор запросов, механизм анализа CodeQL будет запускать набор запросов по умолчанию и любые дополнительные запросы, определенные в дополнительном наборе запросов. Наборы security-extendedsecurity-and-qualityзапросов и для JavaScript содержат экспериментальные запросы. Дополнительные сведения см. в разделе "Об оповещениях при сканировании кода".

Работа с пользовательскими файлами конфигурации
Если вы также используете файл конфигурации для пользовательских настроек, вместо тех, которые указаны в файле конфигурации, используются любые дополнительные пакеты или запросы, указанные в вашем рабочем процессе. Если вы хотите запустить объединенный набор дополнительных пакетов или запросов, префикс значения packsили queriesв рабочем процессе с +символом. Дополнительные сведения см. в разделе "Использование пользовательского файла конфигурации".

В следующем примере +символ гарантирует, что указанные дополнительные пакеты и запросы используются вместе с любыми, указанными в указанном файле конфигурации.

- uses: github/codeql-action/init@v2
  with:
    config-file: ./.github/codeql/codeql-config.yml
    queries: +security-and-quality,octo-org/python-qlpack/show_ifs.ql@main
    packs: +scope/pack1,scope/pack2@1.2.3,scope/pack3@4.5.6:path/to/queries
Использование пользовательского файла конфигурации
Пользовательский конфигурационный файл - это альтернативный способ указать дополнительные пакеты и запросы для запуска. Вы также можете использовать файл, чтобы отключить запросы по умолчанию, исключить или включить определенные запросы и указать, какие каталоги сканировать во время анализа.

В файле рабочего процесса используйте config-fileпараметр initдействия, чтобы указать путь к файлу конфигурации, который вы хотите использовать. В этом примере загружается файл конфигурации ./.github/codeql/codeql-config.yml.

- uses: github/codeql-action/init@v2
  with:
    config-file: ./.github/codeql/codeql-config.yml
Файл конфигурации может быть расположен в репозитории, который вы анализируете, или во внешнем репозитории. Использование внешнего репозитория позволяет указать параметры конфигурации для нескольких репозиториев в одном месте. Когда вы ссылаетесь на файл конфигурации, расположенный во внешнем репозитории, вы можете использовать синтаксис OWNER / REPOSITORY /FILENAME@BRANCH . Например, octo-org/shared/codeql-config.yml@main .

Если файл конфигурации находится во внешнем частном репозитории, используйте external-repository-tokenпараметр initдействия, чтобы указать токен, который имеет доступ к частному репозиторию.

- uses: github/codeql-action/init@v2
  with:
    external-repository-token: ${{ secrets.ACCESS_TOKEN }}
Настройки в файле конфигурации записаны в формате YAML.

Задание пакетов запросов CodeQL
Примечание: Функциональность управления пакетами CodeQL, включая пакеты CodeQL, в настоящее время находится в стадии бета-тестирования и может быть изменена.

Вы указываете пакеты запросов CodeQL в массиве. Обратите внимание, что формат отличается от формата, используемого файлом рабочего процесса.

packs:
  # Use the latest version of 'pack1' published by 'scope'
  - scope/pack1
  # Use version 1.2.3 of 'pack2'
  - scope/pack2@1.2.3
  # Use the latest version of 'pack3' compatible with 3.2.1
  - scope/pack3@~3.2.1
  # Use pack4 and restrict it to queries found in the 'path/to/queries' directory
  - scope/pack4:path/to/queries
  # Use pack5 and restrict it to the query 'path/to/single/query.ql'
  - scope/pack5:path/to/single/query.ql
  # Use pack6 and restrict it to the query suite 'path/to/suite.qls'
  - scope/pack6:path/to/suite.qls
Полный формат для указания пакета запросов scope/name[@version][:path]. Оба versionи pathявляются необязательными. versionэто диапазон версий semver. Если он отсутствует, используется последняя версия. Для получения дополнительной информации о диапазонах semver см. Документы semver по npm.

Если у вас есть рабочий процесс, который генерирует более одной базы данных CodeQL, вы можете указать любые пакеты запросов CodeQL для запуска в пользовательском файле конфигурации, используя вложенную карту пакетов.

packs:
  # Use these packs for JavaScript and TypeScript analysis
  javascript:
    - scope/js-pack1
    - scope/js-pack2
  # Use these packs for Java and Kotlin analysis
  java:
    - scope/java-pack1
    - scope/java-pack2@v1.0.0
Указание дополнительных запросов
Вы указываете дополнительные запросы в queriesмассиве. Каждый элемент массива содержит usesпараметр со значением, которое идентифицирует отдельный файл запроса, каталог, содержащий файлы запросов, или файл определения набора запросов.

queries:
  - uses: ./my-basic-queries/example-query.ql
  - uses: ./my-advanced-queries
  - uses: ./query-suites/my-security-queries.qls
При необходимости вы можете присвоить каждому элементу массива имя, как показано в приведенных ниже файлах конфигурации. Дополнительные сведения о дополнительных запросах см. в разделе "Выполнение дополнительных запросов" выше.

Отключение запросов по умолчанию
Если вы хотите запускать только пользовательские запросы, вы можете отключить запросы безопасности по умолчанию с помощью disable-default-queries: true.

Исключение определенных запросов из анализа
Вы можете добавлять excludeи includeфильтровать в свой пользовательский конфигурационный файл, чтобы указать запросы, которые вы хотите исключить или включить в анализ.

Это полезно, если вы хотите исключить, например:

Конкретные запросы из наборов по умолчанию (security, security-extendedи security-and-quality).
Конкретные запросы, результаты которых вас не интересуют.
Все запросы, которые генерируют предупреждения и рекомендации.
Вы можете использовать excludeфильтры, аналогичные приведенным ниже в файле конфигурации, чтобы исключить запросы, которые вы хотите удалить из анализа по умолчанию. В приведенном ниже примере файла конфигурации оба js/redundant-assignmentзапроса и js/useless-assignment-to-localзапросы исключены из анализа.

query-filters:
  - exclude:
      id: js/redundant-assignment
  - exclude:
      id: js/useless-assignment-to-local
Чтобы найти идентификатор запроса, вы можете щелкнуть предупреждение в списке предупреждений на вкладке Безопасность. Откроется страница сведений об оповещении. Rule IDПоле содержит идентификатор запроса. Дополнительные сведения о странице сведений об оповещении см. в разделе "Об оповещениях при сканировании кода".

Советы:

Порядок фильтров важен. Первая инструкция фильтра, которая появляется после инструкций о запросах и пакетах запросов, определяет, включены или исключены запросы по умолчанию.
Последующие инструкции выполняются по порядку, и инструкции, которые появляются позже в файле, имеют приоритет над более ранними инструкциями.
Вы можете найти другой пример, иллюстрирующий использование этих фильтров, в разделе "Примеры файлов конфигурации".

Дополнительные сведения об использовании excludeи includeфильтрах в вашем пользовательском файле конфигурации см. в разделе "Создание наборов запросов CodeQL". Для получения информации о метаданных запроса, которые вы можете фильтровать, см. раздел "Метаданные для запросов CodeQL".

Указание каталогов для сканирования
Для интерпретируемых языков, поддерживаемых CodeQL (Python, Ruby и JavaScript / TypeScript), вы можете ограничить сканирование кода файлами в определенных каталогах, добавив pathsмассив в файл конфигурации. Вы можете исключить файлы в определенных каталогах из анализа, добавив paths-ignoreмассив.

paths:
  - src
paths-ignore:
  - src/node_modules
  - '**/*.test.js'
Примечание:

Ключевые pathsслова иpaths-ignore, используемые в контексте файла конфигурации сканирования кода, не следует путать с теми же ключевыми словами, которые используются для on.<push|pull_request>.pathsв рабочем процессе. Когда они используются для изменения on.<push|pull_request>в рабочем процессе, они определяют, будут ли выполняться действия, когда кто-то изменяет код в указанных каталогах. Дополнительные сведения см. в разделе "Синтаксис рабочего процесса для действий GitHub".
Символы шаблона фильтра ?, +, [, ], и !не поддерживаются и будут совпадать буквально.
** символы могут быть только в начале или в конце строки или окружены косой чертой, и вы не можете смешивать **и другие символы. Например, foo/**, **/foo, и foo/**/barвсе разрешенные синтаксисы, но **fooэто не так. Однако вы можете использовать одиночные звездочки вместе с другими символами, как показано в примере. Вам нужно будет заключать в кавычки все, что содержит *символ.
Для скомпилированных языков, если вы хотите ограничить сканирование кода определенными каталогами в вашем проекте, вы должны указать соответствующие шаги сборки в рабочем процессе. Команды, которые необходимо использовать для исключения каталога из сборки, будут зависеть от вашей системы сборки. Дополнительные сведения см. в разделе "Настройка рабочего процесса CodeQL для скомпилированных языков".

Вы можете быстро анализировать небольшие фрагменты monorepo при изменении кода в определенных каталогах. Вам нужно будет исключить каталоги на этапах сборки и использовать paths-ignorepathsключевые on.<push|pull_request>слова и для в вашем рабочем процессе.

Примеры файлов конфигурации
Этот файл конфигурации добавляет security-and-qualityнабор запросов в список запросов, выполняемых CodeQL при сканировании вашего кода. Дополнительные сведения о доступных для использования наборах запросов см. в разделе "Выполнение дополнительных запросов".

name: "My CodeQL config"

queries:
  - uses: security-and-quality
Следующий файл конфигурации отключает запросы по умолчанию и указывает набор пользовательских запросов для запуска вместо этого. Он также настраивает CodeQL для сканирования файлов в каталоге src (относительно корневого), за исключением каталога src / node_modules и за исключением файлов, имя которых заканчивается на .test.js . Файлы в src / node_modules и файлы с именами, заканчивающимися .test.js поэтому они исключаются из анализа.

name: "My CodeQL config"

disable-default-queries: true

queries:
  - name: Use an in-repository QL pack (run queries in the my-queries directory)
    uses: ./my-queries
  - name: Use an external JavaScript QL pack (run queries from an external repo)
    uses: octo-org/javascript-qlpack@main
  - name: Use an external query (run a single query from an external QL pack)
    uses: octo-org/python-qlpack/show_ifs.ql@main
  - name: Use a query suite file (run queries from a query suite in this repo)
    uses: ./codeql-qlpacks/complex-python-qlpack/rootAndBar.qls

paths:
  - src 
paths-ignore: 
  - src/node_modules
  - '**/*.test.js'
В следующем файле конфигурации выполняются только запросы, которые генерируют предупреждения об ошибке серьезности. Конфигурация сначала выбирает все запросы по умолчанию, все запросы в ./my-queriesи набор по умолчанию в codeql/java-queries, затем исключает все запросы, которые генерируют предупреждения или рекомендации.

queries:
  - name: Use an in-repository QL pack (run queries in the my-queries directory)
    uses: ./my-queries
packs:
  - codeql/java-queries
query-filters:
- exclude:
    problem.severity:
      - warning
      - recommendation
Настройка сканирования кода для скомпилированных языков
Для поддерживаемых скомпилированных языков вы можете использовать autobuildдействие в рабочем процессе анализа CodeQL для создания своего кода. Это позволяет избежать необходимости указывать явные команды сборки для C / C ++, C #, Go, Kotlin и Java. Для этих языков CodeQL анализирует исходные файлы в вашем репозитории, которые создаются. Для любого из этих языков вы можете отключить autobuildи вместо этого использовать пользовательские команды сборки, чтобы анализировать только файлы, созданные этими пользовательскими командами.

В случае autobuildсбоя или если вы хотите проанализировать набор исходных файлов, отличный от тех, которые были созданы autobuildпроцессом, вам необходимо удалить autobuildшаг из рабочего процесса и вручную добавить шаги сборки. Для проектов на C / C ++, C #, Go, Kotlin и Java CodeQL проанализирует любой исходный код, созданный с помощью указанных вами шагов сборки. Дополнительные сведения о настройке сканирования кода CodeQL для скомпилированных языков см. в разделе "Настройка рабочего процесса CodeQL для скомпилированных языков".

Загрузка данных сканирования кода в GitHub
GitHub может отображать данные анализа кода, созданные извне сторонним инструментом. Вы можете загружать данные анализа кода с upload-sarifпомощью действия. Для получения дополнительной информации см. раздел "Загрузка файла SARIF в GitHub".

Помог ли вам этот документ?

Политика конфиденциальности
Помогите нам сделать эти документы великолепными!
Все документы GitHub имеют открытый исходный код. Видите что-то неправильное или неясное? Отправьте запрос на извлечение.

Или узнайте, как внести свой вклад.

Все еще нужна помощь?
Спросите сообщество GitHub
Обратиться в службу поддержки
© 2023 GitHub, Inc.
Условия
Конфиденциальность
Безопасность
Статус
Справка
Свяжитесь с GitHub
Цены
API разработчика
Обучение
Блог
О нас


README.md

